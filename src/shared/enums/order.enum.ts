export enum OrderStatusEnum {
  PENDING = 'pending',
  DELIVERED = 'delivered',
  RETURNED = 'returned',
  CANCELED = 'canceled',
}

export enum PaymentModeEnum {
  COD = 'cash on delivery',
  ONLINE = 'online',
}
